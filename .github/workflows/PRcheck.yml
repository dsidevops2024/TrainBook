name: PR Workflow

on:
  #workflow_dispatch:
  pull_request:
    paths:
      - './.github/dir1/**'
      - './.github/dir2/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      # Add your workflow steps here
